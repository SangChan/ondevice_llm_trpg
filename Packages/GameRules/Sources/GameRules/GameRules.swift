// GameRules
//
// Phase 2(§40)에서 다음이 이 모듈에 들어온다:
//   - ActionResolver / ResolveResult (자연어 대상 → EntityID 해석)
//   - GameState.visibleEntities(for:) (Resolver의 가시성 화이트리스트)
//   - GameState.affordances(for:) (행동 칩 파생, §31-1)
//
// 지금은 GameCore와의 모듈 경계만 확보한다 — 내용은 비어 있다.
